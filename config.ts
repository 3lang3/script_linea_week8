export const config = {
  /** 最高运行气价 */
  MAX_GAS_PRICE: 5000,
  /** 加价抢跑 */
  ADD_GAS_PRICE: 20,
  /** 是否强制执行到成功 */
  loopUtilSuccess: true,
  /** 账号多的建议换成收费节点的rpc，公用有速率和流量限制 */
  rpc: 'https://linea-testnet.rpc.thirdweb.com'
}